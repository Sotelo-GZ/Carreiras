version: '3.9'
services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
    environment:
      - DATABASE_URL=sqlite:///./carreiras.db
  frontend:
    build: ./frontend
    ports:
      - "5173:80"
    depends_on:
      - backend
